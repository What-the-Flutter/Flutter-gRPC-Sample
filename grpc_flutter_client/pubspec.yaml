name: grpc_flutter_client
description: Demonstration gRPC client

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.1
  flutter_lints: ^2.0.0
  freezed: ^2.1.1
  grpc: ^3.0.2
  protobuf: ^2.1.0


flutter:

  uses-material-design: true
